import { IStoreProps } from "../../types/interfaces";

//export const opportunitiesStateSelector = (state: IStoreProps) => state.opportunities;

export const opportunitiesParamSelector =  (state: IStoreProps) => state.opportunities.param;
